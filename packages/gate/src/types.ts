/**
 * @deprecated This file was created in error; use gate-types.ts instead.
 * Re-exports from gate-types to avoid breaking any stale imports.
 */
export type {
  GateVerdict,
  GateReasonCode,
  GateDecision,
  GateConfig,
  GateSelfTestResult,
} from './gate-types';
export { DEFAULT_FLOOR, DEFAULT_MIN_DELTA } from './gate-types';
